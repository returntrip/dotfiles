// Zed settings
//
// For information on how to configure Zed, see the Zed
// documentation: https://zed.dev/docs/configuring-zed
//
// To see all of Zed's default settings without changing your
// custom settings, run `zed: open default settings` from the
// command palette (cmd-shift-p / ctrl-shift-p)
//
// CHEZMOI TEMPLATE: the bws secret values below are resolved at
// `chezmoi apply` time from Bitwarden Secrets Manager — the real
// keys are never committed to git. Requires .accessToken set in
// chezmoi.toml's [data] section.
//
// This is the Linux (flatpak) wrapper. The native Linux variant lives at
// dot_config/zed/private_settings.json.tmpl; the macOS variant at
// Library/Application Support/Zed/settings.json.tmpl.
{{ template "zed-core" (merge (dict "zed_install" "flatpak") .) }}
