// Zed settings (native Linux wrapper)
//
// Zed reads ~/.config/zed/settings.json for a native (non-flatpak) install,
// e.g. the brew cask `ublue-os/tap/zed-linux`. Content is shared via
// zed-core; only the launcher differs from the flatpak wrapper.
//
// CHEZMOI TEMPLATE: bws secret values resolved at `chezmoi apply` time.
// Requires .accessToken in chezmoi.toml's [data] section.
{{ template "zed-core" (merge (dict "zed_install" "native") .) }}
